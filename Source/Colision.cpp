#include "Colision.hpp"

//template <class T>

bool Colision::verificarColision( Jugador A, Bloque B)
{
		/*
			int x1, x2, y1, y2; //De A
			int x3, x4, y3,y4; // De B
			int w = x2;
			int h = y2;
			
			x1 = A.x;//Of object A, get x
		    x2 = A.x + A.w;	//
		    y1 = A.y;
		    y2 = A.y + A.h;
		
		    //Calculate the sides of rect B
		    x3 = B.alto;  //trabajar con la posicion
		    x4 = B.alto + 60.0f; // (B.w;) //w = width h = height
		    y3 = B.ancho;
		    y4 = B.ancho + 60.0f;  // B.h;
		
		    //Si hay lados de A en B
		    if( y2 <= y3 ){return false;}
		    if( y1 >= y4 ){return false;}
		
		    if( x2 <= x3 ){return false;}
		    if( x1 >= x4 ){return false;}
		    //Si no hay lados de A en B;
		 */   
			return true;
		}
		
